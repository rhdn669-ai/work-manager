rules_version = '2';

// ⚠️ 임시 롤백(2026-07-03) — Firestore 규칙 롤백과 함께 개방으로 되돌림.
// 재조임은 전 클라이언트 최신화 또는 서버 로그인 전환 후 진행.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
