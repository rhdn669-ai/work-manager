# MES 품질 모듈 구상안 — 품질경영 서식 15종 전산화

> 작성 2026-07-30 · 상태: **구상안(인터뷰 4라운드 반영, 코딩 미착수)**
> 대상: IOPN 앱 MES 모드(`workspace = 'production'`) → `/quality`
> 입력자료: 품질경영 서식 15종 (엑셀 12 + PPT 3), 문서번호 IOPNQP-1xx~8xx / IOPNIP-4xx·6xx

---

## 0. 결론 요약 (3줄)

1. **15종 중 신규 개발이 필요한 건 실제로 11종**이다. 나머지 4종(협력사·인원·자재·품목)은 앱에 이미 있는 마스터라 **신규 테이블이 아니라 기존 데이터 연결 대상**이다.
2. 서식 15개를 화면 15개로 만들지 않는다. **양식 엔진 1세트 + 서식 정의(JSON) 15개**. `/quality` 한 페이지에 **탭 6개**로 묶는다.
3. 원본 엑셀 서식을 그대로 재현하지 않는다. **새로 만드는 서류들끼리만 통일된 새 규격**을 쓴다.

---

## 1. 인터뷰 확정 사항 (2026-07-30, 4라운드)

| # | 항목 | 확정 |
|---|---|---|
| 1 | 디자인 기준 | **품질서류 전용 새 디자인** (견적·발주와 별개). 원본 엑셀 서식 재현 불필요 |
| 2 | 1차 용도 | **현장 태블릿·폰 입력이 주력**. PDF는 사무실에서 |
| 3 | 결재란 | **2단** (작성자 → 품질팀장). 4단에서 축소 |
| 4 | 문서번호 | 기존 체계 **그대로 유지** (IOPNQP-104A 등) |
| 5 | 생산↔품질 | **생산 불량 기록이 그대로 품질 대장으로 흘러감** (이중입력 제거) |
| 6 | 검사 트리거 | **발주 입고 → 수입검사 대기열 자동 생성** |
| 7 | 부적합 파생 | **불량자재 이력 자동 등록**만. (협력사평가 자동반영·5M1E 자동연결은 제외) |
| 8 | 교정 알림 | **품질 홈 배지 + 검사 입력 시 경고**. 텔레그램 알림은 제외 |
| 9 | 오프라인 | **불필요** — 사내 와이파이 안정 (온라인 전제) |
| 10 | 채번 | **자동 채번 + 관리자 수정 허용** |
| 11 | 권한 | **입력자 / 품질팀장 / 관리자 3단** |
| 12 | 사진 첨부 | **부적합 시 필수**, 평상시 선택 |
| 13 | 모바일 입력 | **3스텝 위저드** (기본정보 → 검사항목 → 판정·사진) |
| 14 | 모바일 목록 | **카드로 전환** |
| 15 | 서류 색 | **앱과 동일 오렌지 포인트** |
| 16 | 네비게이션 | **탭 활용** (사이드바 하위메뉴 아님) |

---

## 2. 현재 앱 상태 (착수 지점)

| 항목 | 현황 |
|---|---|
| MES 모드 진입 | 로그인 후 워크스페이스 선택 → `sessionStorage.wmWorkspaceMode = 'production'` (`src/utils/workspace.js`) |
| 접근 권한 | `canProduction()` = 관리자 또는 `productionAccess` 체크된 직원 |
| MES 사이드바 | `생산현황`(`/production`) · `품질`(`/quality`) 2개만 — ERP 메뉴 완전 미노출 |
| 생산현황 | **구현 완료**. `productionPanels`, 실시간 구독 + 진행률 자동계산 + **불량사진** + 휴지통 |
| 품질 | **껍데기**. 카드 3개 전부 `준비중` 배지. 서비스·컬렉션 없음 |

→ 라우트·권한·사이드바가 이미 잡혀 있어 **`QualityPage.jsx` 내부만 채우면 되는 상태**다.

---

## 3. 서식 15종 분류 및 통합 판단

### 3-1. 신규 개발 대상 (11종)

| 그룹 | 서식 | 문서번호 | 통합 판단 |
|---|---|---|---|
| **A. 자산** | 계측기 관리대장 | QP-703A | **3종 → 1테이블** |
| | JIG 관리대장 | QP-706A | (`assetType` 구분) |
| | 치공구·TOOL 관리대장 | QP-705A | 스키마 95% 동일 |
| **B. 검사** | 수입검사 성적서 | QP-104A | **문서 → 대장 1:1 쌍** |
| | 수입검사 관리대장 | QP-104B | 성적서 승인 = 대장 1행 |
| | 판넬 출하검사 실적대장 | QP-105B | **2종 → 1테이블** |
| | 부적합List·품질목표 실적대장 | QP-809B | 헤더 70% 동일 |
| | 불량자재 이력관리 List | QP-704C | 부적합 판정 시 **자동 생성** |
| **C. 변경관리** | 5M1E 변경 신청서 | IP-404B | **문서 → 대장 1:1 쌍** |
| | 5M1E 변경점 리스크관리대장 | QP-404A | 신청서 필드 = 대장 컬럼 |
| **D. 인적** | 교육일지 | IP-603D | 참석자 → 직원 마스터 |
| | 자격등급 재평가 신청서 | IP-603E | 승인 시 직원 LEVEL 갱신 |

### 3-2. 기존 데이터 연결 대상 (4종) — 신규 테이블 만들지 않음

| 서식 | 기존 자산 | 처리 |
|---|---|---|
| 협력사 등록대장·평가양식 | `suppliers` | 평가 필드 **추가**. 평가시트만 문서형 신규 |
| 인원 명부 및 인증 기준 | `users` | 자격 필드(LEVEL·부여일) **추가** |
| 자재 입고관리 SHEET | BOM·품목·발주 | 입고내역 **별도 컬렉션 분리**(4,500행) |
| (서식 내 품명·품번) | `purchaseItems` | 품목 선택을 기존 마스터에서 |

> **중복 마스터를 만들면 앱이 두 개로 쪼개진다.** 구매처를 품질에서 또 등록하는 상황은 반드시 피한다.

### 3-3. 테이블로 만들지 않는 것

출하검사의 `품질목표관리`·`월별품질집계`·`품질대시보드`, 수입검사대장 `통합현황`, 계측기·치공구 `교정현황` — 전부 원본 대장의 롤업 수식 시트다. **저장하지 않고 쿼리로 계산**한다.

#### 3-3-1. 필드 전수 명세 (누락 0 검증 완료) ★

서식 15종의 **모든 라벨을 기계적으로 전수 추출**해 별도 문서로 만들었다 → **`MES-QUALITY-FORM-SPEC.md`**

| 항목 | 결과 |
|---|---|
| 총 필드 수 | **433개** (원본 라벨 합계 = 명세 반영 수) |
| 누락 | **0건** — 서식별 대조표로 증명 |
| 추출 방식 | openpyxl로 값·수식 양쪽 로드(자동계산식 원문 확보), Data Validation에서 드롭다운 실제 값 목록 확보, PPT 3종은 `<a:tbl>` 표 구조 + `<a:t>` 전수 추출 |
| 시트 범위 | 대장·집계뷰·대시보드·코드마스터 시트까지 전부 포함 |

서식별 필드 수: F07 협력사 101 · F03 출하검사 64 · F12 5M1E 59 · F09 자격재평가 36 · F11 5M1E신청 37 · F15 수입검사대장 35 · F05 자재입고 31 · F06 계측기 30 · F02 부적합 29 · F01 성적서 24 · F08 교육일지 16 · F14 치공구 16 · F04 불량자재 15 · F10 인원명부 14 · F13 지그 10

**원본 엑셀에서 발견된 결함 (전산화 시 그대로 옮기면 안 되는 것)**
- F03 품질 대시보드 R33 `#REF!` 참조 오류
- F05 자재리스트 "품명" 헤더 중복 오기 (한쪽은 "업체명"이어야 함)
- F13·F14 `구분` 컬럼 드롭다운 소스가 `#REF!`로 깨짐
- F15 J열 "합격수량"(숫자)에 `적합/부적합/N/A` 드롭다운이 걸려 헤더와 불일치

→ 전산화는 **결함을 바로잡을 기회**다. 위 4건은 MES에서 정상 정의로 새로 잡는다.

---

## 3-4. 최종 데이터 구조

**신규 컬렉션 3개**
```
qualityRecords    // 대장·문서 통합. formType 으로 15종 구분, fields 맵에 서식별 값
qualityAssets     // 계측기 · JIG · 치공구 (assetType 구분)
materialReceipts  // 자재 입고내역 (대용량 분리, projectId 정규화)
```
**기존 컬렉션 확장 2개**
```
suppliers  += 평가점수 · 등급 · 승인여부 · 최근평가일
users      += 현재LEVEL · LEVEL1~4 부여일 · 최종부여일
```
서식 정의는 DB가 아니라 **코드 상수**(`src/domain/qualityForms.js`) — git 리뷰·개정 이력이 커밋으로 남는다.

---

## 4. 서류 연계 맵 ★

인터뷰 5~7항의 결론. **한 번 입력한 것이 두 번 입력되지 않게** 하는 것이 이 설계의 목적이다.

### 4-1. 전체 흐름도

```
  [ERP 영역]                    [MES 품질 영역]
  ┌──────────┐
  │ 발주서    │  입고 처리
  │(기존 모듈)│ ─────────────▶ ① 수입검사 대기열 자동 생성
  └──────────┘                    (품명·업체·수량 미리 채워짐)
                                        │
                                        │ 검사자가 태블릿으로 판정
                                        ▼
                                  ② 수입검사 성적서 (문서)
                                        │
                                  결재(2단) 승인
                                        │
                        ┌───────────────┴───────────────┐
                     합격                              부적합
                        │                                │
                        ▼                                ▼
                ③ 수입검사 관리대장            ④ 불량자재 이력 자동 등록
                   (같은 레코드,                   + 사진 필수
                    상태만 전이)                   + 부적합번호 자동채번
                                                     (NCR-2026-00XX)
                                                        │
                                            [수동] 필요 시 5M1E 변경신청

  ┌──────────────┐
  │ 생산현황      │  현장에서 판넬 불량 기록
  │ (기존 구현)   │ ─────────────▶ ⑤ 출하검사·부적합 대장에 자동 반영
  └──────────────┘                    (품질팀은 조치·종결만 추가 입력)

  ┌──────────────┐
  │ 계측기 대장   │ 차기교정일 = 최근교정일 + 교정주기
  │              │ ─────────────▶ ⑥ 품질 홈 "교정 임박" 배지
  └──────────────┘                 + 검사 입력 시 초과 계측기 선택하면 경고
```

### 4-2. 연계 규칙 상세

| 연계 | 트리거 | 동작 | 자동/수동 |
|---|---|---|---|
| ① 발주 → 검사 대기열 | 발주 품목 입고 처리 | 수입검사 대상 1건 생성, 품명·업체·수량·발주번호 프리필 | **자동** |
| ② 성적서 → ③ 관리대장 | 결재 2단 승인 완료 | 같은 레코드의 `status`가 `작성중 → 승인`으로 전이. 대장 목록에 나타남 | **자동** |
| ④ 부적합 → 불량자재이력 | 판정 = 부적합 저장 | 불량자재 이력 행 자동 생성 (개선품 입고일은 나중에 채움) | **자동** |
| ⑤ 생산 불량 → 품질 대장 | 생산현황에서 불량 기록 | 부적합/출하검사 대장에 행 생성. 사진도 함께 넘어옴 | **자동** |
| ⑥ 계측기 상태 | 매일 계산 | 차기교정일 기준 정상/임박(30일)/초과 판정 → 홈 배지 + 검사 입력 시 경고 | **자동** |
| 부적합 → 5M1E | 담당자 판단 | 부적합 화면에서 "변경신청 작성" 버튼 → 프리필된 신청서 | **수동** |
| 부적합 → 협력사 평가 | 담당자 판단 | 평가 시 해당 업체 부적합 건수를 참고 자료로 표시(자동 감점 아님) | **수동** |
| 교육·자격 승인 → 직원 LEVEL | 자격 재평가 승인 | `users`의 LEVEL·부여일 갱신 | **자동** |

### 4-3. ⑤번(생산↔품질)이 이 설계의 핵심

생산현황에는 **이미 불량 기록·사진 기능이 구현돼 있다.** 여기서 기록한 것이 품질 대장으로 흘러가지 않으면, 현장은 같은 불량을 두 번 적게 된다 — 지금 엑셀에서 벌어지는 일이 그대로 앱으로 옮겨질 뿐이다.

**설계 원칙**
- 불량의 **원본(source of truth)은 생산현황**. 현장이 최초 기록한다.
- 품질 대장은 그 기록을 **가져다 쓰고, 품질팀이 조치·검증·종결 필드만 덧붙인다.**
- 즉 품질 대장의 행 = 생산 불량 레코드 참조 + 품질 전용 필드. 복사가 아니라 **연결**이다.

### 4-4. 부적합 하나의 생애주기 (연동 전 과정)

현장에서 부적합 1건이 발생했을 때 데이터가 어떻게 흐르는지 끝까지 추적한 것. **사람이 같은 내용을 두 번 적는 지점이 한 곳도 없어야 한다.**

| 단계 | 누가 | 어디서 | 무엇을 입력 | 자동으로 생기는 것 |
|---|---|---|---|---|
| 1 | 검사자 | 갤럭시탭 현장 | 항목별 합/부 터치 + 부적합 사진 | 부적합번호 `NCR-2026-00XX` 자동채번 |
| 2 | — | 시스템 | — | 불량자재 이력에 행 자동 생성 (품명·업체·LOT은 발주에서 이미 옴) |
| 3 | — | 시스템 | — | 해당 업체 부적합 건수 +1 (협력사 평가 참고자료) |
| 4 | — | 시스템 | — | 품질 홈 "미종결 부적합" 배지 +1, 경과일 카운트 시작 |
| 5 | 품질팀장 | PC | 조치내용·대책 입력 | 조치일 기록 |
| 6 | 품질팀장 | PC | 1~3차 검증 결과 | 검증 이력 누적 |
| 7 | 품질팀장 | PC | 종결 처리 | 배지 −1, 월별 불량지수 재계산, 추이 그래프 반영 |
| 8(선택) | 담당자 | PC | "변경신청 작성" 클릭 | 5M1E 신청서에 부적합 내용 프리필 |

**입력 지점은 딱 3곳** — ①현장 태블릿(검사자) ②PC 조치·검증(품질팀장) ③선택적 5M1E. 나머지는 전부 자동이다.

---

## 4-5. 현장 운영 설계 (갤럭시 탭) ★

작업자가 **장갑 낀 손으로** 공장 조명 아래에서 쓰는 화면이다. 사무실 UI를 축소한 것이 아니라 별도로 설계한다.

### 등록 — 검사 체크 화면

```
[네이비 상단바]  ← 수입검사 체크  IQC-2026-0142        김하늬  07-31 09:14
[스텝]  ①완료 ─── ②현재 ─── ③대기
        기본정보    검사항목    판정·사진

[좌 60% 검사 항목]                    [우 40%]
 01 외관 도장 상태      [합격][부적합]   ┌ 검사 정보 ────┐
 02 치수 정밀도         [합격][부적합]   │ 프로젝트/품명  │
 03 도어 개폐 동작      [합격][부적합]   │ 공급업체       │
 04 접지 터미널         [합격][부적합]   │ 입고 20 / 검사 5│
 05 결선 상태          [합격][부적합]   └───────────┘
    └ 연빨강 배경 + 사진 필수 영역 펼침   ┌ 사용 계측기 ──┐
 06 (미선택)                            │ 버니어 CAL-003 │
                                        │ ⚠ 교정 임박 D-13│
[하단 고정] 5/6 완료 ▬▬▬▬▬░ 83%   [임시저장] [다음 단계]
```

**현장용 규칙**
- 행 높이 최소 80px, 판정 버튼은 폭 넓게 — 장갑 오터치 방지
- **부적합을 누르는 순간** 그 행이 연빨강으로 바뀌고 사진 영역이 즉시 펼쳐짐. 사진 없으면 다음 단계 진행 차단
- 교정 초과·임박 계측기를 선택하면 우측 카드에 주황 경고. 초과면 저장 차단
- 하단 진행률 바로 "몇 개 남았는지"를 항상 보이게
- 임시저장은 언제든 가능 — 현장은 중간에 끊기는 일이 잦다

### 확인 — 부적합 현황 화면

```
[네이비 상단바]  부적합 현황            [주간][월간●][분기]

[요약 4장]  이번달 24건(히어로,▼5) │ 미조치 3 │ 조치완료 21 │ 평균조치 4.2일

[좌 60%] 주차별 발생 추이 (오렌지 영역차트, 4주차 "8건" 강조)
[우 40%] 공정별 비율 도넛 (배선·조립·도장·기타)

[하단] 최근 부적합 2×2 카드 — 번호·현상·품명/담당/일자·상태배지·사진썸네일
       미조치 카드는 좌측 빨간 띠

                                        [＋ 부적합 등록] ← 오렌지 플로팅
```

반장·작업자가 **"우리 공정 요즘 어떤지"를 3초 안에** 파악하는 것이 목적. 그래서 표가 아니라 그래프가 화면의 절반 이상을 차지한다.

---

## 4-6. 스티치 디자인 산출물

스티치 프로젝트 **IOPN MES 품질 모듈** (`453343571192919683`), 디자인 시스템 `IOPN MES 품질`
(네이비 `#002050` + 오렌지 `#f05819`, 판정 3색, 차트 보조색 파랑·보라·청록, 결재 2단 규칙 내장)

| # | 화면 | 기기 | 상태 |
|---|---|---|---|
| 1 | 품질 관제 대시보드 (차트 6종) | PC | **채택** — 범례 한글화·오렌지 추이선 반영 완료 |
| 2 | 품질 홈 (최근등록 표 변형) | PC | 참고 — 준수율 진행바는 살릴 것. 영문 제목은 폐기 |
| 3 | 대장 목록 공통 골격 (계측기 관리대장) | PC | **채택** — 15종에 그대로 복제 |
| 4 | 수입검사 성적서 A4 서식 | 인쇄 | **채택** — 사진 플레이스홀더·결재란 2칸 수정 완료 |
| 5 | **수입검사 체크 (현장)** | 갤럭시탭 | **채택** — 3스텝 위저드, 부적합 시 사진 강제 |
| 6 | **부적합 현황 (추이 확인)** | 갤럭시탭 | **채택** — 주차별 추이·공정별 도넛·2×2 카드·등록 플로팅 |

화면 5·6이 이번 작업의 핵심 신규 산출물이다. 현장이 **등록(5)** 하고 **확인(6)** 하는 두 축.

### 화면별 톤 분리 원칙

한 앱 안에서 **성격에 따라 톤을 나눈다.** 전부 화려하게 하면 표가 안 읽히고, 전부 담백하게 하면 현황 파악이 안 된다.

| 성격 | 화면 | 톤 |
|---|---|---|
| **보는 화면** | 품질 관제, 부적합 현황 | 차트가 화면 60% 이상. 히어로 카드·그라데이션·스파크라인 적극 |
| **다루는 화면** | 대장 목록, 검사 입력 | 정밀·단정. 시각 요소는 게이지·배지 수준으로 절제 |
| **인쇄물** | A4 서류 15종 | 차트·그라데이션 완전 배제. 흑백 인쇄 가능 |

### 미해결

- 대장 목록의 "요약 스트립 + 잔여 게이지 바" 버전은 스티치 생성이 **3회 연속 타임아웃**으로 회수 실패. 현재 채택본은 그 요소가 없는 담백한 버전이며, 기능상 부족함은 없다. 구현 단계에서 직접 추가하면 되므로 재생성에 더 매달릴 필요는 없다.

---

## 5. 통일 표준 (서류를 손보는 기준)

원본 엑셀 서식은 재현하지 않는다. **새로 만드는 서류 15종끼리만** 아래 규격을 공유한다.

### 5-1. 판정값 통일 ★

| 현재 서식별 표기 | MES 표준 |
|---|---|
| 합/불, 적합/부적합, OK/NG, 최종판정, 종합판정 | **합격 · 부적합 · 보류** 3값 |
| 등급(A~D), 조치등급, 잔여등급 | 별도 필드 `grade` 로 분리 |

배지 색: 합격 `#15803d` / 부적합 `#dc2626` / 보류 `#6b7280`. 앱 전체 동일.

### 5-2. 서류 3층 구조 ★

```
① 문서 메타   문서번호 · Rev · 시행일        → 상단 작게 (입력 불필요, 정의에서 자동)
② 본문        서식별 필드                    → 서식마다 다른 유일한 부분
③ 결재선      작성자 → 품질팀장 (2단)         → 공통 컴포넌트 1개, 하단 우측
```

### 5-3. 공통 필드 사전

| 표준 필드 | 서식별 현재 표기 | 연결 대상 |
|---|---|---|
| `itemName` / `itemNo` | 품명 / 품번·PartNo·도면No·자재코드 | `purchaseItems` |
| `projectNo` | 프로젝트번호 · 설비명 · 프로젝트설비 | 기존 프로젝트 |
| `inspectedAt` / `inspector` | 검사일 / 검사자 | `users` |
| `supplierId` | 공급업체 · 업체명 · 공급자명 | `suppliers` |
| `owner` | 담당자 · 관리담당자 · 확인자 · 작성자 | `users` |

### 5-4. 채번 규칙

| 대상 | 형식 | 예 |
|---|---|---|
| 부적합보고서 | `NCR-{연도}-{4자리}` | NCR-2026-0031 |
| 수입검사 | `IQC-{연도}-{4자리}` | IQC-2026-0142 |
| 5M1E 변경 | `CHG-{연도}-{4자리}` | CHG-2026-0008 |
| 계측기·JIG·치공구 | `CAL/JIG/TOL-{3자리}` | CAL-003 |

자동 채번이 기본, **관리자만 수정 가능**.

---

## 6. 화면 설계

### 6-1. 네비게이션 — 탭 방식

사이드바는 `생산현황 / 품질` 2개 그대로 두고, **`/quality` 한 페이지 안에서 탭으로 전환**한다.

```
/quality
 ├─ [자산관리]  계측기 · JIG · 치공구 (하위 소탭 3)
 ├─ [수입검사]  성적서 + 관리대장
 ├─ [출하·부적합]  출하검사실적 + 부적합List + 불량자재이력
 ├─ [변경관리]  5M1E 신청서 + 리스크관리대장
 ├─ [교육·자격]  교육일지 + 자격등급 재평가
 └─ [협력사평가]  평가시트 (등록대장은 기존 구매처)
```
탭에 건수 배지 표시. 서식 15개를 그대로 나열하지 않고 **업무 흐름 단위 6묶음**으로.

### 6-2. 품질 홈 (`/quality` 진입 시)

준비중 카드 3개를 아래로 교체한다.

```
┌─ KPI 4장 ─────────────────────────────────────────────┐
│ 당월 불량지수   수입검사 합격률   미종결 부적합   교정 임박 │
│   0.42 ▼        98.2% ▲          3건 ⚠          2건 ⚠   │
└───────────────────────────────────────────────────────┘
┌─ 탭 6개 (건수 배지) ───────────────────────────────────┐
┌─ 좌 65%: 최근 등록 ──────┐┌─ 우 35%: 교정 임박 / 미종결 ─┐
```
KPI는 저장값이 아니라 대장 쿼리 결과다(3-3항).

### 6-3. 대장 공통 골격 ★ — "통일감"의 실체

**모든 대장이 이 골격 하나를 공유한다.** 달라지는 건 표의 컬럼과 모달의 본문 필드뿐이다.

```
① 헤더      서식명 + [QP-703A Rev.2] 배지   (우측) [휴지통] [엑셀] [+ 신규]
② 필터바    기간 · 프로젝트 · 업체 · 판정 · 검색
③ 표        공통 앞컬럼(No·일자·프로젝트·품명) + 서식별 컬럼 + 판정배지 + [작업]
④ 요약줄    건수 · 합격/부적합 집계
⑤ 입력      PC=모달 / 모바일=3스텝 위저드
```

**모바일**: 표 → **카드 전환**. 카드에는 일자·품명·판정배지만 크게, 나머지는 탭하면 상세.

기존 앱 관례 준수:
- 작업 컬럼 `className="col-action"` — **우측 정렬**, 삭제는 `btn btn-sm btn-danger`
- 삭제는 전부 `trashGeneric()` 경유 — **영구삭제 금지**, 영구삭제는 관리자만
- `TrashModal` + `TrashList` 재사용, `TRASH_TYPE_LABEL`·`TRASH_TABS`에 신규 타입 등록
- 색·버튼·간격은 `DESIGN-SYSTEM.md` 토큰 (오렌지는 주행동 1곳만)
- 4종 디바이스(아이폰·갤럭시·폴더블·PC) 반응형 필수

### 6-4. 모바일 검사 입력 — 3스텝 위저드

```
1/3 기본정보    프로젝트 · 품명 · 업체 · 수량   (발주 연동 시 이미 채워짐)
2/3 검사항목    항목별 합/부 선택 (큰 터치 버튼)
3/3 판정·사진   종합판정 · 사진첨부 · 비고 · 저장
```
- 상단에 스텝 인디케이터(●─○─○), 하단에 큰 [다음] 버튼
- **부적합 선택 시 사진 없으면 저장 차단**
- 교정 초과 계측기를 선택하면 경고 표시

### 6-5. 문서형 서식 (성적서·신청서)

별도 페이지를 만들지 않는다. **대장의 "신규" 입력과 같은 화면**을 쓴다.
- 저장 → `qualityRecords` 1건 (상태: 작성중)
- 결재 2단 완료 → 상태 `승인` → **같은 레코드가 대장 목록에 나타남**
- PDF 출력은 기존 `bizPdf` 패턴 재사용. **원본 엑셀 레이아웃 재현 불필요** — 새 통일 서식으로

### 6-6. 자산 특수 로직

```
차기교정일 = 최근교정일 + 교정주기(월)
상태 = 정상(30일 초과) / 임박(30일 이내, 주황) / 초과(경과, 빨강)
```
→ 품질 홈 "교정 임박" 배지 + 검사 입력 시 경고와 직결.

---

## 7. 권한 (3단)

| 역할 | 대상 | 가능 |
|---|---|---|
| **입력자** | 검사자·현장 (`productionAccess`) | 작성 · 본인 문서 수정 · 조회 |
| **품질팀장** | 품질보증팀장 | 위 전부 + 승인(결재 2단) · 종결 · 타인 문서 수정 |
| **관리자** | `role === 'admin'` | 위 전부 + 영구삭제 · 채번 수정 · 기준정보(서식 정의) 관리 |

기존 `canProduction()`에 품질 역할 필드를 얹는다.

---

## 8. 단계별 적용 계획

각 단계는 **독립 배포 가능**하다.

| 단계 | 범위 | 산출물 | 왜 이 순서 |
|---|---|---|---|
| **1** | 양식 엔진 + 자산 3종 | 공통 골격·결재 2단 컴포넌트, `qualityAssets`, 자산관리 탭 | 3종 스키마 동일 → 엔진 검증 최적. 교정 임박 알림으로 즉시 가치 |
| **2** | 수입검사 | `qualityRecords`, 발주 입고 연동①, 문서→대장 전이②, 모바일 위저드 | 이중 입력 제거 효과가 가장 큼 |
| **3** | 출하·부적합 | 생산현황 연계⑤, 불량자재 자동생성④ | **연계 설계의 핵심**. 2단계 구조 재사용 |
| **4** | 5M1E 변경관리 | 신청서→대장, 리스크 5×5 자동판정 | 독립성 높음 |
| **5** | 교육·자격 | `users` 확장 | 직원 마스터 변경 → ERP 영향 검토 |
| **6** | 협력사평가·자재입고 | `suppliers` 확장, `materialReceipts` | 구매 모듈 영향 범위가 가장 넓어 마지막 |
| **7** | 품질 대시보드 | KPI 4장 + 타임라인 | 1~6 데이터가 쌓인 뒤 의미 있음 |

---

## 9. 남은 결정 사항

| # | 쟁점 | 상태 |
|---|---|---|
| 1 | 자재 입고내역 4,500행 — Firestore 유지 vs Supabase 이전 대기 | **6단계에서 결정** (온프레미스 이전 방침과 충돌) |
| 2 | 전자결재(승인 대기·알림)를 실제로 돌릴 것인가 | 1단계는 **기록만**. 발주 승인선(#25)과 함께 별도 과제 |
| 3 | 엑셀 원본 보관 위치 | **자료실(FileLibrary)에 원본 보관, MES를 정본**으로 |
| 4 | 생산현황 불량 스키마에 품질 필드를 어떻게 얹을지 | 3단계 착수 전 상세 설계 필요 |

---

## 10. 이 구상안이 없앤 것

- 자산 3종 → 1테이블 (스키마 동일)
- 검사 대장 2종 → 1테이블 (헤더 70% 동일)
- 문서 4종 → 대장의 상태 전이 (별도 페이지 없음)
- 마스터 4종 → 기존 앱 데이터 연결 (신규 개발 0)
- 집계 시트 6개 → 쿼리 (저장 안 함)
- **생산 불량 재입력 → 자동 연계** (현장 이중입력 제거)

**결과: 서식 15종 · 시트 40여 개 → 컬렉션 3개 + 기존 확장 2개 + 탭 6개.**
