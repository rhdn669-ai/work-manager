// 품목 표의 칸 폭 배분(%) — BOM 화면표와 구매발주서.
//
// 합이 정확히 100 이어야 한다. 넘치거나 모자라면 어느 한 칸이 찌그러지거나 사라진다.
// 실제로 도번 열을 늘리면서 발주서 배분을 함께 늘리지 않아, 규격이 받아야 할 몫을
// 메이커가 가져가고 규격은 83px 로 찌부러져 있었다 (2026-09-02 대표님 「BOM 규격 칸
// 좀더 확장」에서 드러남). 그래서 배분을 한 곳에 모으고 합을 테스트로 붙든다.
//
// 규격이 가장 넓다 — 「3110SB-05W-B49-E0F0-DA」처럼 이 표에서 제일 긴 값이다.

/** BOM 화면표 — 타입(형번) 열이 있을 때 */
export const BOM_COLS_WITH_VARIANT = [
  1, // 여백
  2, // No
  6, // 코드
  6, // 도번   ┐ 나란히 놓이는 두 칸이라 폭을 같게 (2026-09-02 대표님)
  6, // BOX    ┘
  9, // 타입 — 드롭다운(「T5391 / MT8311」)이 들어가야 해서 넓게 (2026-09-03 대표님)
  11, // 품명
  5, // 메이커 — 「Honeywell」 정도면 읽힌다. 남는 몫은 비고로 (2026-09-22 대표님)
  13, // 규격
  4, // 분류 — 좁을 때의 최소 폭은 CSS .bom-cat-col 이 잡는다 (2026-09-22 대표님)
  4, // 구분 (도급·사급 토글 버튼)
  4, // 수량
  5, // 단가
  5, // 합계
  7, // 구매처
  8, // 비고 — 손으로 적는 칸이라 넓어야 쓴다 (2026-09-22 대표님 「메이커 칸 줄이고 비고 칸 늘려」)
  4, // 작업
];

/** BOM 화면표 — 타입 열이 없을 때(타입 몫은 규격과 품명이 나눠 갖는다) */
export const BOM_COLS_NO_VARIANT = [
  1, // 여백
  2, // No
  6, // 코드
  6, // 도번   ┐ 폭을 같게
  6, // BOX    ┘
  12, // 품명
  5, // 메이커 — 「Honeywell」 정도면 읽힌다. 남는 몫은 비고로 (2026-09-22 대표님)
  19, // 규격
  4, // 분류 — 좁을 때의 최소 폭은 CSS .bom-cat-col 이 잡는다 (2026-09-22 대표님)
  4, // 구분 (도급·사급 토글 버튼)
  4, // 수량
  5, // 단가
  5, // 합계
  7, // 구매처
  10, // 비고 — 손으로 적는 칸이라 넓어야 쓴다 (2026-09-22 대표님)
  4, // 작업
];

/** 구매발주서 품목표 */
// 코드 열은 발주서에서 뺐다 — 어느 줄이나 같은 앞머리(IO…)만 나와 읽을 것이 없고,
// 그 5% 가 짤리던 품명·규격을 눌렀다 (2026-09-12 대표님 「글이짤려… 코드는 표시x」).
export const PO_COLS = [
  2, // No
  6, // 도번   ┐ 폭을 같게
  6, // BOX    ┘
  14, // 품명
  6, // 메이커
  18, // 규격
  3, // 분류
  4, // 수량 — 재고를 쓰는 줄은 «줄 그은 필요 수 / 실제 발주 수» 두 줄이 들어간다
  5, // 재고 — 「11 사용」 배지가 칸을 넘어가 잘렸다 (2026-09-22 대표님 「재고 버튼 글이 밖으로 벗어남」)
  7, // 단가   ┐ 금액은 한 글자도 잘리면 안 된다 — 「2,005,30…」으로 끊겨 있었다
  8, // 합계   ┘ (2026-09-12 대표님)
  8, // 기본 구매처 — 「(주)천음테크」가 들어가야 한다
  5, // 비고
  4, // 입고
  4, // 작업
];

/** BOM 화면표 — 타입별 수량으로 옮긴 뒤. 「타입」 열이 없어지고 그 몫을 수량과 규격이 받는다.
 *  수량 칸 안에서 「공통 · 타입1 · 타입2 …」로 나뉘므로 타입이 셋 이상이면 조금 더 넓힌다
 *  (2026-09-21 대표님 「수량 아래에 박스 타입 달고 리스트에 타입별 수량 바로 보이게」). */
export function bomColsTypeQty(variantCount = 2) {
  const extra = Math.max(0, (Number(variantCount) || 0) - 2) * 2;
  return [
    1, // 여백
    2, // No
    6, // 코드
    6, // 도번
    6, // BOX
    11, // 품명
    5, // 메이커 — 「Honeywell」 정도면 읽힌다. 남는 몫은 비고로 (2026-09-22 대표님)
    13 - extra, // 규격 — 타입이 늘면 수량에 내준다
    4, // 분류 — 좁을 때의 최소 폭은 CSS .bom-cat-col 이 잡는다 (2026-09-22 대표님)
    4, // 구분
    13 + extra, // 수량 — 「공통 · 타입…」이 나란히 들어간다
    5, // 단가
    5, // 합계
    7, // 구매처
    8, // 비고 — 손으로 적는 칸이라 넓어야 쓴다 (2026-09-22 대표님)
    4, // 작업
  ];
}

export function sumOf(cols) {
  return cols.reduce((s, n) => s + n, 0);
}

/** 재고 표 — 사급·도급이 나란히 놓이므로 칸 폭을 «한 곳에서» 정한다.
 *  따로 적어 두었더니 남음 칸이 한쪽은 9%, 한쪽은 13% 로 어긋나 있었다
 *  (2026-09-12 대표님 「도급 사급 문자열 폭 통일해주고」).
 *  null 은 「남은 공간 전부」 — 규격이 가장 길어 그 자리를 준다. */
export const STOCK_COLS = [
  '44px', // No
  '10%', // 도번 — 남음 칸에 자리를 내줬다 (2026-09-15 대표님 「도번 칸 줄이고」)
  '14%', // 품명
  null, // 규격
  '6%', // 1대당
  '7%', // 나감
  '156px', // 남음 — 고정폭: 숫자 위·금액 아래 두 줄 + 옆에 「수정」이 늘 한 칸에 (2026-09-15 대표님
  //         「금액표시 3열쓰지말고 … 2줄에 다들어가게 남음칸 고정값」)
  '8%', // 가능 SET — 남음에서 나오는 결론이라 남음 뒤에 둔다 (2026-09-12 대표님)
  // 비고는 이제 «눌러서 창»이라 표에는 글만 보인다 — 좁혀도 되고, 그만큼 「이번 입고」에 준다
  // (2026-09-18 대표님 「이번입고 안짤리게 … 비고는 입력할때 모달로」)
  '8%', // 비고 — 품목마다 한 줄 메모
  '12%', // 이번 입고 — 숫자칸 + 「넣기」가 한 줄에 들어가야 한다
];
