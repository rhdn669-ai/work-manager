# work-manager 디자인 시스템 (대규모 재편성 기준)

> 토스(invest-signal 적용분) 스타일 참고 · 테마색 **주황 유지**.
> **제1원칙 = 통일감.** 강조 사이즈·방식·색상·형태를 전 페이지에서 동일하게.
> 이 문서는 재편성 작업의 **단일 기준(source of truth)**. 페이지 구현은 모두 여기에 맞춘다.
> 확정일: 2026-06-18 (대표님 인터뷰 7라운드 결과)

---

## 0. 작업 방식
1. **디자인 시스템(토큰 + 공용 컴포넌트 CSS) 먼저 확정** → 2. 멀티 에이전트로 페이지별 일괄 적용 → 3. 교차 검토(검토·검토·검토).
- 토큰은 `src/styles/global.css :root`에 이미 존재 → 이를 확장/정리해서 사용(중복 변수 생성 금지).
- 페이지 개별 인라인 스타일·하드코딩 색상/간격 제거 → 토큰 변수로 치환.
- **작업 중 추가 지적(인터럽트)이 들어와도 현재 진행 중인 작업을 전부 마무리한 후 다음 지시를 처리한다.** 부분 완료 상태로 방향 전환 금지.

---

## 1. 네비게이션 / 구조
| 구분 | 결정 |
|---|---|
| **모바일 메인 네비** | 하단 탭바(토스식) — **근태 · 구매 · 발주 · BOM · 더보기** 5탭 |
| 더보기 탭 내용 | 외주, 자료실, 마감, 직원/구매처 관리 등 나머지 도메인 |
| **PC 네비** | 좌측 사이드바 유지 + 리디자인(토스 톤) |
| 활성 탭/메뉴 | 주황(선택·활성만 주황 원칙) |

## 2. 표 → 모바일 전환 (반응형)
- 조회용 리스트: **768px 미만에서 카드 리스트(토스식)** 로 전환.
- 대량 편집표(BOM 편집표 등): 모바일에서 **가로 스크롤 유지**(카드 전환 X).
- 페이지 특성별로 위 둘 중 선택하되, 같은 성격 페이지는 동일 방식.

## 3. 휴지통 (모든 저장 페이지 공통)
- **모든 저장 페이지에 휴지통 필수.**
- 진입: 각 페이지 **우측 상단 '휴지통' 버튼 → 모달**.
- 모달 내부: 삭제 항목 스냅샷 리스트 + **복원 / 영구삭제** (버튼 우측 끝 정렬).
- `trash` 컬렉션 스냅샷 방식 유지(type 필터로 페이지별 분리). **단일 공용 컴포넌트**로 통일.

---

## 4. 색상 규칙 (통일의 핵심)
> ⚠️ 구버전 충돌 정리(v58.9): §4·§24 일부가 재편 이전 기준(주황=--primary)을 혼재 → **§26이 현재 CSS 기준 정확**.
- CSS 실제 토큰: **`--primary: #002050`(네이비)** = 주행동·타이틀면. **`--accent: #f05819`(오렌지)** = 활성·선택·배지 포인트.
- **오렌지(`--accent` #f05819)는 오직 3곳에만**: ① 주행동 버튼 1개 ② 선택된 탭/카드 ③ 활성 상태.
- 그 외 텍스트·테두리·배경·보조버튼은 **전부 무채색(회색 계열)**.
- **상태 배지**: 의미색을 **연한 톤(palm)** 으로 — 연한 배경 + 진한 글자.
  - 대기 = 회색 / 진행중 = 파랑 / 완료 = 초록 / 취소·경고 = 빨강.
  - 오렌지는 상태배지에 쓰지 않음(오렌지는 주행동·활성에만).

## 5. 버튼 시스템
- **한 화면에 주행동(오렌지 채움, `btn-primary`)은 1개만.**
- 보조 버튼 = 흰/회색 **아웃라인**.
- **삭제 버튼**: 반드시 `btn btn-sm btn-danger` + 아이콘(`<Icon name="trash" className="btn-ic"/>`) + 텍스트 "삭제". 앱 전체 동일 스타일(연분홍 bg·빨강 글자). `btn-ghost`, `btn-danger-text`, `closing-delete`, `library-file-btn delete` 등 비표준 클래스 사용 금지.
  - `btn-danger` 토큰: bg `#fde7e7`, fg `#c53030`, border `#f6d0d0` — **절대 솔리드 빨강(#dc2626)으로 채우지 않음**.
- 크기·높이 토큰 통일: **`--btn-h-sm 36px / -md 44px / -lg 52px`** (CSS 실제값 기준. 구버전 32/38/46은 폐기).
- **액션 버튼(추가·복원·삭제·수정·휴지통)은 행/영역의 우측 끝에 항상 정렬**(총괄 MD 규칙).
- **액션 버튼 2열 금지**: `.row-actions`, `.btn-group` 은 반드시 `flex-wrap: nowrap`. 버튼이 줄바꿈되면 레이아웃 버그로 간주.
- **표 "작업"(액션) 컬럼 표준 — 정석 위치 (전 앱 통일)**: 헤더 글자·셀 버튼 모두 우측 끝 정렬.
  - 적용: 작업 컬럼 `<th>`/`<td>` 에 `className="col-action"` (단일버튼 셀 포함). 버튼 묶음 wrapper는 `.btn-group` 또는 `.row-actions`.
  - 수치값: 정렬 `text-align:right` / `justify-content:flex-end`, 셀 우측 패딩 **16px(PC) · 12px(≤768) · 10px(≤480)**, 버튼 간격 `gap:8px`.
  - 정의 위치: `global.css` "표 작업(액션) 컬럼 — 전 앱 단일 표준" 블록. `.table td .btn-group`도 flex-end로 표준화.
  - 제외: `.bom-project-action-col`(구매처·외주·BOM·견적 9% 그리드), `.bom-action-col`(발주 품목표) — 고유 폭 시스템 유지(이미 우측 정렬).
- **표 열 정렬 표준 — 머리글과 값은 반드시 같은 쪽 (전 앱 통일, 2026-08-08)** ※ 훅(check-forbidden)이 강제
  - 새 표를 만들거나 열을 추가할 때, `<th>`와 `<td>` 중 **한쪽에만** 정렬을 주면 머리글은 왼쪽인데 값은 가운데처럼 **어긋나 보인다.** 반드시 짝으로 지정한다.
  - 공용 클래스로만 지정한다 (`global.css` "표 열 정렬 표준" 블록):
    | 열 성격 | 클래스 | 정렬 |
    |---|---|---|
    | 수량·단가·금액·재고 등 숫자 | `col-num` | 가운데 + `tabular-nums` |
    | 단위·구분 등 짧은 라벨 | `col-unit` | 가운데 |
    | 작업(버튼) | `col-action` | 오른쪽 |
    | 그 밖의 글자 | (없음) | 왼쪽 = 기본값 |
  - `col-num` 안의 `input`/`.num-input`, `col-unit` 안의 `input`/`select`는 클래스가 알아서 맞춘다. **셀마다 `text-align`을 따로 쓰지 말 것.**
  - 페이지 전용 CSS로 머리글만 다시 덮지 말 것 (구매처·외주 작업 칸이 이 때문에 어긋나 있었음).
  - 자가 점검: 브라우저 콘솔에서 `<th>`와 첫 행 `<td>`의 computed `textAlign`을 비교해 다르면 버그.
- **표 입력칸 단일 표준 (전 앱 통일, 2026-08-10 대표님 확정)**
  - 구매(품목·BOM·발주)와 품질 대장이 높이·글자·정렬·포커스가 제각각이라 같은 앱처럼 보이지 않던 것을 한 벌로 맞췄다.
  - **높이 32px · 글자 13px · 모서리 6px · 안쪽 여백 6/8px · 정렬 전부 가운데 · `tabular-nums`**
  - 평소엔 테두리 없이 비어 보이고, 마우스를 올리면 옅은 바탕, **누르면 네이비 테두리**(주황은 주행동 버튼 전용).
  - 정의 위치: `global.css` 「표 안 입력칸 — 전 앱 단일 표준」 블록 + `quality.css` `.q-grid-table` (같은 값).
  - **화면별 CSS로 높이·정렬을 따로 덮지 말 것.** 한 곳을 고치면 두 화면이 함께 바뀐다.
  - 품질 대장만 격자선(`border`)을 유지한다 — 숫자·날짜가 많아 칸 경계가 필요하다.
- **자료실(lib-detail-table) 컬럼 분배**: name=100%, type/date/size/action=각 116px(type·date·size), action=140px. 모바일(≤720px): type 숨김, date/size=78px, action=88px.

## 6. 모서리 · 그림자
- 둥글고 은은한 그림자(토스). 카드 = `--radius-md(14)` ~ `--radius-lg(18)`, `--shadow`.
- 딱딱한 네모/진한 그림자 금지.

## 7. 타이포 (5단계 일관)
| 역할 | 크기 | 굵기 |
|---|---|---|
| 페이지 제목 | `--font-xl`(20) | 700 |
| 섹션 제목 | `--font-lg`(16) | 600 |
| 본문/표 | `--font-md`(14)~`--font-base`(13) | 400 |
| 보조 | `--font-sm`(12) | 400 |
| 캡션 | `--font-xs`(11) | 400 |
- 굵기는 **700 / 600 / 400** 3종만.

## 8. 간격 (4px 그리드)
- 4 / 8 / 12 / 16 / 24 / 32 (`--space-1`~`--space-8`)만 사용.
- 카드 내부 16, 카드 간 12, 섹션 간 24 기준.

---

## 9. 항목 수정 · 이동 (반응형 편집)
- 이름·분류 등 항목은 **가능한 광범위하게 수정·이동 가능**하게.
- 방식: **인라인 클릭 편집**(텍스트 클릭 → input 전환) + **드래그 핸들로 순서 이동**.
- **순서 이동은 관리자만**(일반직원은 드래그 핸들 숨김).

## 10. 상태 화면 (로딩/빈/에러)
- 로딩 = 회색 **스켈레톤**(깜빡).
- 빈 화면 = 아이콘 + 안내문구 + 주행동 버튼.
- 에러 = 안내 + 재시도 버튼.
- **단일 공용 컴포넌트 1세트**로 전 페이지 공유.

## 11. 알림 / 피드백
- **결과(저장·삭제 성공/실패) = 토스트**(성공 회색 · 실패 빨강). native `alert` 전면 제거.
- **폼 입력 검증 오류 = 인라인**(입력칸 밑 빨강 메시지).

## 12. 삭제 확인
- native `confirm` 전면 제거 → **통일 확인 모달**(제목 + 경고문 + 취소(회색)/삭제(빨강)).
- 휴지통 있는 항목은 "삭제 = 휴지통 이동" 안내 포함.

## 13. 아이콘
- **선(line) SVG 아이콘 세트로 통일**. 이모지(✕, 🗑 등) 제거.
- 공용 아이콘: 휴지통·수정·이동(핸들)·추가·검색·닫기·복원·다운로드 등.

---

## 14. 페이지 헤더 (반응형)
- PC: 한 줄 — **제목(좌) · 검색/필터(중) · 주행동+휴지통(우측 끝)**.
- 모바일: 2줄로 자연스럽게 접힘. **액션은 항상 우측 끝**.

## 15. 폼 레이아웃
- **라벨 위 + 입력칸 아래(세로)**. 모바일·PC 동일.
- 입력 UI는 native `prompt` 대신 **Modal**(총괄 MD). 라벨-입력칸 간격 겹침 금지.

## 16. 숫자 · 날짜 포맷 (일반 회계 관행)
- 금액·수량 = **천단위 콤마**. 정렬은 §5 표 열 정렬 표준을 따른다 — **표 안에서는 가운데**(2026-08-10), 카드·요약 등 표가 아닌 곳은 우정렬.
- 날짜 = `YYYY.MM.DD`.

## 17. 권한별 화면
- **권한 없는 기능은 숨김(아예 안 보임)** — 비활성 회색 X.
- 일반직원: 자료실 미공개(권한 토글), 순서이동·삭제·휴지통 등 숨김.

## 18. 인쇄 출력물 (BOM · 발주서)
- 기존 흑백·페이지네이션·정렬 로직은 **건드리지 않음**(검증된 양식).
- 단, 머릿글·테두리 등에 **화면과 동일 톤(주황 포인트)** 소폭 적용해 통일감만 부여.

## 19. 다크모드
- ~~함께 구축~~ → **v58.x에서 완전 제거(대표님 결정).** `[data-theme="dark"]` 규칙 85개 삭제, ThemeToggle 컴포넌트 삭제, main.jsx에서 강제 라이트 모드 고정. 다크모드는 이 앱에 없는 기능.

---

## 20. 달력(근태) 인원 표시
- 날짜 셀 = **인원수 배지(숫자)** 로 간결하게 (이름 칩 나열 X).
  - 예: `근무 5 · 잔업 2 · 연차 1` — 셀 공간에 맞춰 숫자 위주.
- 날짜 **클릭 시 아래 명단**에 이름 전체 표시(기존 상세 리스트 영역 활용).
- 구분 **3종**: 근무 · 잔업 · 연차.
  - 색은 상태배지 연한톤 규칙: 근무=파랑, 잔업=주황계열은 피하고 회/보조색, 연차=초록 등 의미색 연하게(주황 주행동 원칙 유지).
  - ※ '근무' 인원 집계는 현재 달력 데이터(잔업·연차)에 없음 → 출근/근태 소스 연결 필요(구현 시 확인).

## 21. 지문(생체) 로그인 — A안: 기기별 빠른 로그인 (이번 작업 포함)
- 방식: **WebAuthn**(지문·Face/Touch ID·Windows Hello). HTTPS(Cloudflare)에서 동작.
- 현 인증 구조 전제: 커스텀 로그인(`users` 컬렉션 `code` 조회 + 평문 `password` 비교, localStorage `workManagerUser` 세션). Firebase Auth 아님.
- **동작 = "이 기기에 등록된 세션을 생체로 잠금해제"** (서버 검증 아님, 편의 계층).
  1. 1회 정식 로그인(코드+비밀번호) 후 "이 기기에서 지문 사용" 등록 → 플랫폼 credential 생성, 자격정보 ID + 세션을 로컬에 저장.
  2. 다음부터 로그인 화면에 "지문으로 로그인" 보조 버튼 → `navigator.credentials.get()` 생체확인 성공 시 저장 세션 복원.
- **로그인 화면 기본은 항상 코드+비밀번호.** 지문은 등록된 기기에서만 보이는 보조 버튼.
- **기기당 등록 계정 = 1개**(개인폰 가정). 다른 계정 지문 등록 시 이전 것 교체.
- **계정 전환** = 로그아웃 또는 "다른 계정으로 로그인" 링크 → 정식 로그인.
- 제약: 기기마다 1회 등록 / 기기에 생체 등록돼 있어야 / 지문은 "누구"인지 구분 못 함(계정 선택 아님).
- ※ 서버검증 정식 패스키(B안)는 비밀번호 해싱·Firestore 규칙 강화(보안 부채)와 **묶어서 별도 단계**에 진행.

## 22. 프로젝트 체크(공수 입력) UX — 직원 최우선 개선
> 직원이 가장 어려워하는 지점. 현 방식: 잔업등록=긴 `<select>` 드롭다운(AttendancePage.jsx:123), 마감=1~31일 가로 그리드(SiteClosingPage). 모바일에서 특히 고통.
> 원칙: 직원 머릿속("오늘 ○○현장 1공수")을 **1~2탭**으로. 쉽고·효율·세련(토스).

**방향: 모바일 = 달력 도장 / PC = 강화 그리드** (반응형 분기)

- **① 프로젝트 = 큰 둥근 칩 카드 선택**(드롭다운 폐기): 내 담당·최근·★즐겨찾기 상단 노출, 탭 1번 선택, 검색 한 줄. (토스 "최근 이체대상" 패턴)
- **② 달력 1탭 체크인(모바일)**: 날짜 탭 → 프로젝트 칩 탭 → 공수 프리셋(0.5/1/2 버튼) → 자동저장+토스트. 가로 그리드 대신 세로 흐름("출근 도장").
- **③ 효율 단축**: "어제와 동일" · "이번 주 평일 일괄" 버튼 / 상시현장(양산) 자동채움 유지 → 예외 날만 손댐 / 공수 0.5·1·1.5·2 프리셋(키패드 최소화).
- **④ 진행률**: "이번 달 22일 중 18일 입력됨" 프로그레스.
- **⑤ 세련 디테일**: 선택 칩 색 dot+체크 애니메이션, 누름 scale(0.98), 자동저장 토스트, 부드러운 ease.
- **⑥ PC = 강화 그리드 유지**: 셀 클릭 시 프리셋 팝오버, 행 일괄채움, 드래그채움(기존 유지).

## 23. 디테일 점검 — 통일 드롭다운(Select)
- 네이티브 `<select>`는 기기(특히 삼성인터넷)마다 드롭다운이 제각각 렌더 → 첫 항목이 트리거에 겹쳐 "튀어나오는" 글리치 발생.
- → **통일 커스텀 드롭다운 `Select`**(`components/common/Select.jsx`)로 전면 교체.
  - 트리거 아래 **6px 간격**으로 열림(겹침 방지), 아래 공간 부족 시 위로 자동 플립, max-height 스크롤, 바깥클릭·Esc 닫힘.
  - 선택 항목 = 네이비(`primary-soft` 배경·체크 아이콘) — "선택=네이비(--primary)" 원칙.
- ✅ **완료(v45.1): 전 앱 네이티브 `<select>` 0개** — 23개 파일 페이지 + FixedExpenseModal·HintSettingModal 전부 통일 Select로 교체(멀티에이전트). 폼 안에서는 전체 너비 채움 보정.
- 교훈: "이런 디테일 놓치지 말 것" — 네이티브 컨트롤(select/date/file)은 기기별 렌더 차이가 크므로 통일 컴포넌트로 감싼다.

## 24. 브랜드 로고 · 색 (ICNP 육각 엠블럼 반영)
> ⚠️ 구버전 충돌 정리(v58.9): 재편 중간단계에서 --primary를 오렌지로 기재했으나, 최종 확정은 **--primary=네이비, --accent=오렌지** (§26·CSS 실제값 기준).
- 로고: 업로드된 `ICNP_logo_final.pdf` (벡터) → 트림·고해상도 투명 PNG로 추출.
  - `public/icnp-emblem.png`(헤더·로그인), `public/icnp-icon-192/512.png`·`icnp-icon.png`·`icnp-icon-maskable.png`(파비콘/PWA).
- **브랜드 색 = 로고 색 (CSS 실제 토큰 기준)**:
  - **네이비 `#002050`** → **`--primary`** (주행동 버튼·타이틀 면·사이드바). `--navy`도 동일값.
  - **오렌지 `#f05819`** → **`--accent`** (활성·선택·배지 포인트). hover `#d94810`, soft `#feefe7`, tint `#fbddd0`.
  - meta theme-color `#002050`. 사이드바 배경 = 네이비 그라데이션, 활성 = 흰 알약+오렌지 글자.
- ⚠️ **명칭은 IOPN 유지**(회사 (주)아이오피엔, 품목코드 IOPN-xxx). 로고 글자가 ICNP라 명칭까지 ICNP로 갈지는 **대표님 확인 필요** — 현재 헤더는 엠블럼만 노출, 텍스트 워드마크 없음.

## 25. 구현 완료 현황 (v59.0 기준)
- ✅ 모바일 하단 **고정 5탭 + 더보기 바텀시트** (홈·잔업·연차·팀·더보기). 권한별 더보기 메뉴.
- ✅ **지문 로그인(WebAuthn A안)** — 로그인 화면 버튼 + 유저메뉴 등록/해제. 기기당 1계정.
- ✅ **통일 휴지통** — 범용 `trashGeneric()` + `<TrashModal>` + `subscribeTrashByType`. 연결: 발주·BOM·구매처·프로젝트·직원·외주·이벤트·부서·견적·품목·자료실파일/폴더·운행기록. 각 페이지 우상단 휴지통 버튼→모달, 복원/영구삭제.
- ✅ **통일 Select 드롭다운** — 전 앱 네이티브 `<select>` 0개.
- ✅ **다크모드 완전 제거** — v58.x. ThemeToggle 삭제, main.jsx 강제 라이트 고정.
- ✅ **발주 시스템** — 대기(draft) 상태·칸반 5열, 공장 관리, 업체별 PDF 출력·발주완료표시. 모든 업체 발주완료 시 대기→발주 자동 전환.
- ✅ **자료실** — Windows Explorer 스타일(PC 브레드크럼·테이블), 드릴다운 모바일, DnD 폴더 이동.
- ✅ **삭제 버튼 전체 통일** — `btn btn-sm btn-danger`(연분홍 bg·빨강 글자). 비표준 클래스 제거.
- ✅ **ICNP 브랜드** — 육각 엠블럼 파비콘/PWA 아이콘 반영.
- ⏳ **남음**: 프로젝트 체크 UX(모바일 달력 도장/PC 그리드), 페이지 헤더·빈화면·에러 공용 컴포넌트, 이모지→SVG 아이콘 잔여.

## 26. 컨트롤 크기 · 버튼 마크업 표준 (통일)
> 원칙: **지시 하나 받으면 같은 맥락 전체 점검 후 일괄 적용** (부분 적용 금지).

- **색 비중**: 기본색 = **네이비(#002050, --primary)** = 주행동·제목·면. 포인트 = **오렌지(#F05010, --accent)** = 활성·선택·배지 등 작은 점만.
- **컨트롤 높이**: 입력칸(input/select/textarea/통일드롭다운) = **44px**. 기본 버튼(btn) = **44px**(입력과 정렬). btn-sm = 36px(헤더/인라인), btn-lg = 52px. textarea 최소 88px.
- **같은 줄 버튼은 같은 크기**: 한 헤더/툴바/모달 안 버튼은 동일 size 클래스. (마감 헤더 '목록'만 btn-sm 빠졌던 버그 수정함)
- **휴지통 버튼**: `<button className="btn btn-sm btn-outline"><Icon name="trash" className="btn-ic"/>휴지통</button>` (이모지 금지).
- **추가/등록 주행동**: `<button className="btn btn-primary btn-sm"><Icon name="plus" className="btn-ic"/>{라벨}</button>` (라벨 리터럴 "+" 금지, 아이콘이 + 역할).
- 헤더 액션 = `.page-actions`(휴지통 좌 · 주행동 우, 우측 끝 정렬).

## 28. 표(Table) 컬럼 간격 · 정렬 기준

> CSS 기반 실제값 기준. 이 섹션이 없어서 컬럼 간격·정렬이 페이지마다 제각각이었던 문제를 해소하기 위해 추가(v59.1).

### 기본 셀 패딩 (global.css `.table th/td`)
| 위치 | 패딩 | 높이 |
|---|---|---|
| 헤더(`th`) | `9px 10px` | `36px` |
| 데이터 셀(`td`) | `9px 10px` | `36px` |

### 컬럼 유형별 정렬 · 폭 기준
| 컬럼 유형 | 정렬 | 권장 폭 | 클래스/방법 |
|---|---|---|---|
| 이름·내용(주 컬럼) | **좌정렬** | `100%` (남은 공간) | 기본 |
| 번호(No/순번) | 가운데 또는 좌 | `40–60px` | `style={{ width: 48 }}` |
| 날짜(`YYYY.MM.DD`) | 좌정렬 | `100–120px` | `style={{ width: 110 }}` |
| 상태 배지 | 좌정렬 | `80–140px` | `style={{ width: 100 }}` |
| 작업(액션) | **우정렬** | **140px (PC) / 88px (≤480px)** | `className="col-action"` (패딩 16/12/10px) |

### 컬럼 간 시각적 구분이 부족할 때
- 인접한 두 컬럼이 시각적으로 붙어 보이면 → **더 넓은 쪽 컬럼의 `paddingLeft: 24px`** 추가.
- 예: "품목" 컬럼 옆 "발주 상태" 컬럼 → `td/th style={{ paddingLeft: 24 }}`.
- **절대 `margin`으로 해결하지 말 것** — table cell은 margin 무효.

### 가로 여백이 남을 때 — 컬럼 채움 방향 원칙
> 화면 너비가 넓어 오른쪽에 빈 공간이 생길 때의 처리 기준.

1. **좌측부터 채운다** — 컬럼은 왼쪽부터 자연스럽게 늘어나고, 남는 공간은 오른쪽(빈 영역)으로 밀린다.
   - 이름·내용 컬럼에 `width: 100%`(또는 `flex: 1`) 부여 → 이 컬럼이 남는 너비를 모두 흡수.
   - 고정폭 컬럼(날짜·금액·상태·작업 등)은 폭 유지, 이름 컬럼만 늘어남.

2. **2열 레이아웃 금지** — 여백이 많아도 표를 2열로 나누거나 `column-count`·CSS Grid 멀티칼럼으로 재배치하지 않는다.

3. **간격 조절로 해결** — 여백이 너무 넓어 보기 불편하면:
   - 이름 컬럼의 `min-width`를 늘리거나 `paddingLeft`를 추가해 시각적 균형 조정.
   - 컨테이너에 `max-width`를 지정해 표가 과도하게 넓어지는 것을 제한.
   - 예: `<div className="table-scroll-x" style={{ maxWidth: 1200 }}>` 등.

4. **기본 패딩은 유지** — 셀 기본 패딩(`9px 10px`)은 건드리지 않는다. 개별 셀 간격은 `paddingLeft/Right`로만 미세 조정.

```
✅ 올바른 예시
  [이름(100%)] [날짜(110px)] [금액(110px)] [작업(auto)]   ← 이름이 여백 흡수

❌ 잘못된 예시
  [이름] [날짜]          ← 2열로 나누거나
  [금액] [작업]             빈 공간에 다른 컬럼 배치
```

### 현재 앱 적용 특수 컬럼 체계
| 컬럼 클래스 | 사용처 | 특성 |
|---|---|---|
| `.col-action` | 전 앱 작업 컬럼 | 우정렬, **140px (PC) / 88px (≤480px)**, 패딩 16/12/10px, nowrap |
| `.bom-action-col` | 발주 품목표 | 우정렬, padding-right 6px, 별도 폭 시스템 |
| `.bom-project-action-col` | 구매처·외주·BOM·견적 | 9% 그리드, 우정렬 |
| `lib-detail-table` 컬럼 | 자료실 | type/date/size=116px, action=col-action 공통값, 모바일 축소 |

---

## 27. 적용 체크리스트 (페이지마다)
- [ ] 색: 주황은 주행동/선택/활성에만, 나머지 무채색
- [ ] 버튼: 주행동 1개만 주황, 보조 아웃라인, 삭제 빨강, **우측 끝 정렬**
- [ ] 휴지통 버튼(우상단) + 통일 모달
- [ ] 표→모바일 카드 / 대량편집표 가로스크롤
- [ ] 상태 화면(스켈레톤·빈·에러) 공용 컴포넌트
- [ ] 토스트 + 인라인 검증 / 확인 모달 / 선 SVG 아이콘
- [ ] 헤더(제목·검색·액션 우측) / 폼(라벨 위) / 숫자·날짜 포맷
- [ ] 권한 없는 기능 숨김
- [ ] 4종 반응형(아이폰·갤럭시·폴더블·PC) + 다크모드
